[package]
name = "protocol"
version = "0.1.0"
edition = "2021"
description = "Shared wire protocol between the blocker GUI (unprivileged) and blockerd (root)."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
